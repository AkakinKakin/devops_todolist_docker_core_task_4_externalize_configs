apiVersion: v1
kind: Service
metadata: 
  name: todoapp-nodeport
  namespace: todoapp
spec:
  type: NodePort
  selector:
    app: todolist
  ports:
    - port: 80
      target_port: 8080
      nodePort: 30080
      